import { CollapseTextDirective } from './collapse-text.directive';

describe('CollapseTextDirective', () => {
  it('should create an instance', () => {
    const directive = new CollapseTextDirective();
    expect(directive).toBeTruthy();
  });
});
